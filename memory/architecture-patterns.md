# Architecture Patterns

## Portal Isolation
- Use (portal_name) folders in Expo Router to isolate domain-specific logic while sharing the /lib engine.

## Hybrid Routing
- Use complexity and prompt length to decide between cloud and local AI providers.
